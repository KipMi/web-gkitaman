import React from "react";
import FormPendeta from "./FormPendeta";
import TabelPendeta from "./TabelPendeta";

const page = () => {
  return (
    <div>
      <FormPendeta />
      <TabelPendeta />
    </div>
  );
};

export default page;
